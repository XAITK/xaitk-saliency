coverage:
  status:
    project:
      default: false
      tests:
        target: 90%
        paths:
          - "tests/"
      package:
        target: 90%
        paths:
          - "!tests/"
    patch:
      default:
        target: 90%
